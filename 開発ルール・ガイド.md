# 開発ルール・ガイド

## 開発環境（必須ツールとバージョン）

- **コンテナ実行基盤:** Docker / Docker Compose（`docker compose`コマンドが使える環境、Docker Desktop または Docker Engine + Compose v2 を推奨）
- **バックエンド（コンテナ内）:** Python 3.12（`python:3.12-slim` を使用）
  - フレームワーク / ライブラリ:
    - Flask
    - psycopg[binary] == 3.2.3
    - pydantic == 2.10.2
    - pydantic-settings == 2.6.1
- **データベース（コンテナイメージ）:** PostgreSQL 16 (`postgres:16-alpine` を使用)
- **フロントエンド（コンテナ内）:** Node.js 20 (`node:20-alpine` を使用）
  - ランタイム / ビルドツール:
    - React 18.3.1
    - react-dom 18.3.1
    - react-router-dom 6.28.0
    - Vite 5.4.11
    - TypeScript 5.6.3
    - @vitejs/plugin-react 4.3.3

注: 開発・プレビューは Docker Compose 上で完結するため、ホスト側に Python や Node を個別にインストールする必要は基本的にありません。ローカルで直接実行したい場合は上記のランタイムバージョンを揃えてください。

## フレームワークとランタイムの解説

### 共通

- **Docker**: アプリケーションをコンテナとして実行するための基盤。開発者ごとの差異を減らし、同じ実行環境を再現しやすくする。
- **Docker Compose**: 複数コンテナをまとめて定義・起動するためのツール。DB、バックエンド、フロントエンドの依存関係を一括で管理できる。よく使うコマンドは `docker compose up` / `docker compose down`。
- **Dev Container**: VS Code などからコンテナ内をそのまま開発環境として使うための仕組み。ローカル PC の設定差異を減らし、Python や Node のバージョン、拡張機能、作業ディレクトリをそろえやすい。

### バックエンド

- **Python 3.12**: バックエンド実装に使うプログラミング言語と実行環境。可読性が高く、Web API やデータ処理の学習に向いている。
- **Flask**: Python 製の軽量な Web フレームワーク。ルーティングやリクエスト処理をシンプルに実装しやすく、小規模な API や学習用途でも扱いやすい。
- **psycopg**: PostgreSQL に接続するための Python ドライバ。プレースホルダ付き SQL とパラメータを組み合わせて安全にクエリを実行でき、`DATABASE_URL` で接続先を指定する。
- **Pydantic**: リクエストやレスポンスのデータを型安全に扱うためのライブラリ。入力値の検証やデータ整形をシンプルに書ける。
- **pydantic-settings**: 環境変数や `.env` から設定を読み込むためのライブラリ。設定値を型付きで安全に管理できる。

### フロントエンド

- **Node.js 20**: フロントエンド開発で使う JavaScript 実行環境。Vite や npm スクリプトの実行基盤になる。
- **React**: UI をコンポーネント単位で構築するためのライブラリ。画面を部品ごとに分けて再利用しやすくできる。
- **react-dom**: React で定義した UI をブラウザの DOM に描画するためのライブラリ。React アプリを実際に画面へ表示する役割を持つ。
- **react-router-dom**: React アプリで画面遷移を扱うためのライブラリ。URL に応じてページコンポーネントを切り替えられる。
- **Vite**: フロントエンドの開発サーバ兼ビルドツール。起動が速く、ホットリロードも軽快で、`npm run dev` で開発サーバを立ち上げられる。
- **TypeScript**: JavaScript に型を追加した言語。フロントエンド実装時の入力ミスや設計の揺れを減らしやすい。
- **@vitejs/plugin-react**: Vite 上で React を快適に動かすためのプラグイン。JSX 変換や開発時の更新体験を支える。

### DB

- **PostgreSQL 16**: 本プロジェクトのデータストアとして使う RDBMS。堅牢で SQL 標準に近く、学習用途でも実務用途でも扱いやすい。
- **init.sql**: コンテナ起動時に初期データを投入するための SQL ファイル。Compose のボリュームマウント経由で読み込ませ、初期状態をそろえる。

## モノレポ構成の考え方

本課題は、1つのリポジトリでフロントエンド、バックエンド、DB 初期化 SQL、関連ドキュメントをまとめて管理するモノレポ構成を前提とする。

モノレポ構成の主な意図

- フロントエンド、バックエンド、DB、ドキュメントを同じ履歴で管理できるため、仕様変更と実装変更の対応関係を追いやすい。
- `docker-compose.yml` をリポジトリのルートで一元管理しやすく、開発環境の起動手順をそろえやすい。
- Phase ごとの作業で複数ディレクトリにまたがる変更が発生しても、1つのブランチと Pull Request でまとめてレビューしやすい。

## 開発フォルダ構成

開発フォルダ構成は以下のとおり。

```text
project-root/
├─ docker-compose.yml
├─ 開発者ガイド.md
├─ backend/
│  ├─ Dockerfile
│  ├─ requirements.txt
│  └─ app/
│     ├─ __init__.py
│     ├─ config.py
│     └─ ※役割毎のコード
├─ db/
│  └─ init.sql
└─ frontend/
   ├─ Dockerfile
   ├─ index.html
   ├─ package.json
   ├─ tsconfig.json
   ├─ vite.config.ts
   └─ src/
      ├─ api.ts
      ├─ App.tsx
      ├─ main.tsx
      ├─ styles.css
      └─ pages/
         ├─ MembersPage.tsx
         └─ ※各ページごとのコード
```

- `backend/`: Flask ベースの API 実装を配置する。
- `db/`: PostgreSQL 初期化 SQL や DB 関連ファイルを配置する。
- `frontend/`: React + Vite ベースのフロントエンド実装を配置する。

補足

- ルートディレクトリはモノレポの起点として扱い、`docker compose` や Git 操作は原則としてこの階層で実行する。
- `.devcontainer/` を追加する場合は、リポジトリ全体を開発対象にできるよう、ルート直下または対象ディレクトリごとに設定を置く。

## 開発・運用ルール

- **Docker / docker-compose:** 開発および実行は `docker-compose.yml` を使って行う。ローカル環境は Docker 上で構築し、サービスの起動・停止・再作成は Docker Compose で行うこと。
- **Dev Container:** VS Code の Dev Containers を利用する場合は、コンテナ内の作業ディレクトリとホスト側のリポジトリがマウントされていることを理解したうえで使うこと。コンテナ内で編集した内容はホスト側のファイルにも反映される前提で作業する。
- **バージョン管理:** ソースコードは Git / GitHub で管理する。
- **ブランチ運用ルール:**
  - `main` ブランチはドキュメントのモックのみを格納する。実装コードは含めないものとする。
  - Phase0 では `main` から `develop` ブランチを作成し、モノレポ構成フォルダ、Docker / Docker Compose、必要に応じて Dev Container などの開発環境自体を整えること。
  - Phase1 以降の開発は `develop` ブランチから `feature/<短い説明>` の命名規則で feature ブランチを作成して行い、作業完了後に Pull Request を作成してレビューを受けること。

### Dev Container の基本的な使い方

Dev Container を使う場合の基本的な流れは以下のとおり。

1. リポジトリを VS Code で開く。
2. `.devcontainer/devcontainer.json` がある場合は「Reopen in Container」を実行し、定義されたコンテナ内で作業を開始する。
3. コンテナ起動後は、ターミナルやエディタがコンテナ内環境を使っていることを確認してから、依存ライブラリのインストールやアプリ起動を行う。
4. バックエンド、フロントエンド、DB を連携して確認する場合は、必要に応じてリポジトリルートで `docker compose up --build` を実行する。

Dev Container 利用時の注意点

- `workspaceFolder` やマウント設定により、どのディレクトリを作業ルートとして開いているかが変わるため、起動前に設定内容を確認する。
- `frontend/` と `backend/` を個別に開く構成と、リポジトリ全体を開く構成では、見えるファイル範囲や実行コマンドの起点が異なる。
- Dev Container はエディタの開発環境をそろえる仕組みであり、アプリ全体のサービス起動そのものは `docker compose` で管理する運用でもよい。

### Phase0 の開発ブランチ作成手順

Phase0 では、`main` を起点に `develop` ブランチを作成し、開発環境の土台を整える。

基本手順

```sh
git switch main
git pull origin main
git switch -c develop
git push -u origin develop
```

Phase0 で整備する対象の例

- モノレポ構成のベースディレクトリ
- `docker-compose.yml`
- `backend/`、`frontend/`、`db/` の初期構成
- 必要に応じて `.devcontainer/` などの開発補助設定

### Phase1 以降の開発ブランチ作成手順

Phase1 以降の課題に着手する際は、`main` では作業せず、必ず `develop` を最新化したうえで feature ブランチを作成してから進める。

基本手順

```sh
git switch develop
git pull origin develop
git switch -c feature/<短い説明>
```

ブランチ名の例

- `feature/phase1-db`
- `feature/phase2-api`
- `feature/phase3-frontend`
- `feature/phase4-batch`

補足

- 1つの Phase に対して 1ブランチを基本とする。
- 作業完了後はコミットを整理したうえで Push し、Pull Request を作成してレビューを受ける。
- 途中で別作業を始める場合は、作業内容ごとに新しい feature ブランチを切る。

## 進捗報告と課題実践の着目点

### 進捗報告

- 課題の期限は、Phase3 までを 4月末までとする。
- `概要.md` に記載された Phase のうち、現在どの Phase を進めているかを明記する。
- 各作業日ごとに、対象 Phase の進捗率や完了した内容、未着手または残作業を報告する。 ※進捗率は各Phase毎、%は感覚でよい。
- あわせて、完了見込み日を記載する。
- 作業日ごとに、作業終了時点の内容を必ず GitHub 上のリモートリポジトリへ Push する。
- 今回は、実行エラーが残っている状態でも、その日の作業内容が分かるところまで進んでいれば Push してよい。
- Phase1 の実装が完了したら、その時点で完了報告と Push、Pull requestの作成 を必ず行う。レビュアーの設定を忘れずに。
- 進捗報告の場は Teams 上でよい。

報告例

- `2026-03-17`: Phase2 を実施中。一覧取得 API と登録 API まで完了。進捗は 60% 程度。残りは更新・削除 API と動作確認。完了見込みは `2026-03-19`。

### 課題実践時の着目点

- 実装中に自分で調べたことを記録する。
- AI に質問して確認した内容も含めてよい。
- 途中経過のメモは簡潔でよいが、最終的には「何を調べ、どう理解し、実装にどう反映したか」が分かる形で残す。

進捗報告に含める観点

- 調べたこと
- 理解したこと
- 実装に反映したこと

記録例

- psycopg で日付範囲検索の SQL を組み立てる方法を確認し、プレースホルダとパラメータを使って `from` と `to` の条件を一覧検索に反映した。
